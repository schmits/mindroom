# PR #1586 Final Fable 5 Review

Claude Fable 5 (`claude-fable-5`) performed the final independent zero-tolerance review in a separate agent-cli worktree.

The review compared base `7098e66daec37df4ea511060541db69aeebece97` with candidate head `605e6725e0db1c3a7318e54d8046ea1c701be906`.

The isolated reviewer committed its complete report as `03a80a37076d099b23fd962f3b9fddbd44a4573c` without pushing or merging.

## Verdict

**APPROVED**

The reviewer found no remaining blocker and no caveat attached to the approval.

## Prior findings and resolution

The first independent Fable 5 review at candidate head `58baed616cecaf57f4a9eb817c4b9bca2fd81efe` required two low-severity changes.

The first finding identified duplicate identical sync-cache normalization helpers in `src/mindroom/matrix/cache/thread_writes.py`.

Commit `82dcffb14` removed the duplicate and routed threaded and plain sync events through the single existing normalizer without changing classification behavior.

The final reviewer verified that no stale helper reference remains and that the sync-cache and backend contract suites exercise the consolidated path.

The second finding identified that checked evidence recorded joined members while the committed harness did not reproduce that authenticated membership query itself.

Commit `605e6725e` added authenticated `GET /_matrix/client/v3/rooms/{room_id}/joined_members`, exact expected-account comparison after any invited join, fail-closed rejection before media or event work, sorted member IDs in raw evidence, regression tests for success and rejection, and explicit historical provenance documentation.

The final reviewer verified endpoint authentication, request ordering, exact membership, evidence serialization, failure behavior, and the distinction between the pinned historical run and current harness behavior.

## Independent validation

- `uv run pytest tests/test_matrix_event_cache_live_audit.py -n 0 --no-cov` passed 17 tests, including real `ffprobe` validation and both membership regressions.
- `uv run pytest tests/test_matrix_cache_interaction_contract.py -n 0 --no-cov` passed 42 tests with real SQLite and PostgreSQL parametrizations.
- The focused Matrix cache, thread-domain, membership, reaction, and backend suite passed 386 tests.
- `uv run tach check --dependencies --interfaces` passed.
- A definitive full `uv run pytest -q` rerun exited zero with no failed or error lines.

The reviewer also re-inspected the complete base-to-head production and test diff, including event-family gating, stale-index validation, redaction fallback, point-cache filtering, consumer failure handling, backend parity, documentation accuracy, and Tach exports.

## Rejected nonissues

The reviewer rejected a raw-evidence schema-version bump because the field is additive, raw evidence is write-only, and artifact provenance pins the producing harness commit.

The reviewer rejected the lack of a dedicated invited-two-account success test because authenticated sorted success, fail-closed rejection ordering, and invite-token identity are each pinned at their owning seams.

The reviewer rejected changing the historical request count because the evidence document pins the earlier harness commit and explicitly distinguishes its out-of-band authenticated membership capture from current harness behavior.

The reviewer rejected requiring the invited client to repeat the membership query because the joined-member map is room-wide and the invited token identity is independently authenticated before joining.

The reviewer observed the known unrelated parallel CLI manifest-test flake, verified that test and its complete file pass serially, and obtained a clean definitive full-suite rerun.

## Constraints

The reviewer touched no production system, external Matrix room, credential, GitHub state, source file, test file, or product documentation.

The isolated review changed only its ignored `.claude/REPORT.md`, committed that report, and did not push or merge.
